import { StyleSheet } from "react-native"; 

const GuardaRegistroStyles = StyleSheet.create({
    form:{
        paddingHorizontal: 30
    },
    buttonContainer:{
        padding: 40
    },
    loading: {
        position: 'absolute',
        bottom: 0,
        top: 0,
        right: 0,
        left: 0
    }
});

export default GuardaRegistroStyles;